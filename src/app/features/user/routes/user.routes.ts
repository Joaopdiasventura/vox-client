import { Routes } from '@angular/router';
import { AccessPage } from '../access/access-page/access-page';

export const userRoutes: Routes = [
  {
    path: 'access',
    component: AccessPage,
  },
];
